name: ac_estimate_app
description: "에어컨 견적서 프로그램 - 대운공조시스템"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^8.0.0
  intl: ^0.19.0
  pdf: ^3.11.0
  printing: ^5.13.0
  path_provider: ^2.1.2
  provider: ^6.1.1
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/fonts/
